"""VibeLevel Aura — standalone scoring for real, self-directed AI work sessions.

This package has its own model definitions, scoring pipeline, signal extractor,
and persistence (`aura_session`). It reuses only platform infra (DB pool, LLM
client, auth).
"""
